<%= form_for(@blog) do |f| %>
  <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    Name of your blog :
    <%= f.text_field :title %>
  </div>
  <div class="field">
    Describe your blog in few words :
    <%= f.text_area :body %>
  </div>
  <div class="field">
     Link it :
      <%= f.text_field :link %>
  </div>
  <div class="field">
    What city does your blog capture? :
      <%= f.text_field :city_name %>
  </div>
  <div> <b>Upload Profile Picture:</b> <%= f.file_field :picture %> </div><br/>
    <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
